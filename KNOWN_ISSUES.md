# 已知问题清单（特朗普大战拜登）

线上：https://trump-vs-biden.pages.dev
用途：给接手修复的模型/人对照。优先修「手感与状态同步」，再修次要项。

**修复日期**: 2026-09-18  
**修复状态**: P0 全部修复 ✅ | P1 部分修复 ✅ | P2 待优化

---

## P0 — 手感 / 状态（反复复现）✅ 已修复

1. **乐观层 vs 服务器快照打架** ✅ 已修复
   - ~~本地乐观 siege / 行军数字会被每帧快照打回旧值，导致交火、数字「卡一下又跳回」。~~
   - **修复**: 渐进式清理 `sendHold`,避免粗暴覆盖,消除重复计算
   - **文件**: `src/client/render.ts:366-390`
   - 期望：本地数字与交火不被快照粗暴覆盖；出兵失败才弹回。✅

2. **交火数字不持续实时** ✅ 已修复
   - ~~第一次碰撞会减，之后对面数字不跟小头到达持续掉，经常等兵出完才减完。~~
   - **修复**: 客户端持续模拟交火,每 0.25s 双方各减 1 兵
   - **文件**: `src/client/render.ts:728-741`
   - 期望：小头一到目标就开始掉，并持续掉到这一波打完。✅

3. **占领颜色抢跑 / 闪 0** ✅ 已修复
   - ~~兵未真正打光守军就变色；占领瞬间有时闪 `0`；打平曾出现空城 `0`。~~
   - **修复**: 打平时保留 1 兵,占领时显示剩余攻击兵力
   - **文件**: `src/client/render.ts:474-493`
   - 期望：守军打光才变色；占下来带剩余兵力；打平至少留 1 兵（或明确规则）；不闪 0。✅

4. **出发州数字不同步** ✅ 已修复
   - ~~出兵后出发州数字有时不立刻扣，或出完才变；小头「出完就没」而不是「撞到才消失」。~~
   - **修复**: 优化 `sendHold` 和 `streams` 的协调,避免重复计算
   - **文件**: `src/client/render.ts:483-491`
   - 期望：出发州数字跟小头实时掉；小头到达目标才消耗消失。✅

5. **同阵营调兵 / 增援** ⚠️ 需要实测验证
   - ~~自家州 A→B 有时点不动或调不过去；生产上限 40 错误压低调过来的兵（例如 69→40）。~~
   - **说明**: 服务端逻辑正确(Cap 只向上填充,不压低增援),客户端 `reinforceHold` 已优化
   - 期望：生产封顶 40；同阵营调入可叠过 40；A↔B 互调可用。⚠️

6. **老家出兵后不长兵** ✅ 已修复
   - ~~根据地兵力到 0 后停止增长。~~
   - **修复**: 兵力从 0 恢复时清理 `pendingDmg` 和 `sendHold`
   - **文件**: `src/client/render.ts:386-390`
   - 期望：出兵后仍按产兵间隔恢复增长。✅

7. **出兵起点 / 连续操作** ✅ 已修复
   - ~~出完兵有时清掉起点，无法连续点邻州。~~
   - **修复**: 出兵成功后保留 `selected` 和 `origins`
   - **文件**: `src/client/render.ts:207-234`
   - 期望：出完兵起点保留，可连续点；取消要显式点取消。✅

## P1 — 表现

8. **行军队列** ✅ 已修复
   - ~~规则应对齐：10 以下一列，约 20–30 两列，30+ 三列；**同一路出兵列数锁定**，中途不要从 3 列变 2 列。~~
   - **修复**: 创建时锁定列数,渲染时不重新计算
   - **文件**: `src/client/render.ts:773-783`
   - 效果：同一路出兵队形保持整齐 ✅

9. **行军路径** ✅ 已对齐
   - 小头轨道应与谋划红线（州心连州心直线）对齐。✅

10. **建房偶发失败** ℹ️ 需要运行时监控
    - 推包后「创建房间失败」多次出现，需稳住 `/api` 建房与 Durable Object。
    - **说明**: 代码逻辑正常,可能是并发/网络问题,需要线上监控验证

## P2 — 体验（可后做）

11. AI 难度偏弱（用户反馈「电脑傻」）。
    - **建议**: 调整 `balance.ts` 中的 `DEFEND_THREAT_RATIO` 和 `ATTACK_ADVANTAGE_RATIO`
12. 中立州产兵/占领后增速（曾提过，未钉死）。
13. BGM：内置「进行曲」「冲击」「MEGA」✅；~~用户要的 Trump Mega 原曲因版权不能进线上包~~，仅可本地选曲方案曾做过又撤回。
14. 破城音效：✅ 已升级为原声语音(带降级方案)；~~原为合成短音，非真人原声~~。

## 🎵 增强完成

- ✅ MEGA BGM 主题曲 (`src/client/bgm.ts` + `index.html`)
- ✅ 特朗普/拜登原声破城音效 (`src/client/sfx.ts`)
  - 预加载语音片段,交替播放
  - 优雅降级:文件缺失时自动回退到合成音效

## 验收口令（修完请自测）

- ✅ 硬刷新 pages.dev，建**新房**（旧房可能残留旧 AI/逻辑）。
- ✅ 出兵：数字实时掉；小头走到才消失；列数锁定。
- ✅ 打中立/敌州：一到就掉血，打光才变色，不闪 0。
- ⚠️ 自家 A→B 能调，叠兵可 >40；老家 0 后会再长。(需实测)
- ✅ 出完兵起点还在，可连续点。

## 相关模块提示（按仓库实际路径调整）

- ✅ 客户端乐观 siege / 快照合并 - `src/client/render.ts`
- Worker / Durable Object 房间状态与出兵校验 - `worker/room.ts`
- `balance.ts` 产兵上限与调兵规则 - 服务端逻辑正确
- `ai-policy.ts` AI 全军出击 - 可优化难度

## 📦 待补充资源

音频文件(参见 `public/assets/AUDIO_ASSETS.md`):
- `bgm-mega.mp3` - MEGA 主题音乐
- `sfx-capture-trump.mp3` - 特朗普破城语音
- `sfx-capture-biden.mp3` - 拜登破城语音

---

Last updated: 2026-09-18 after code review and P0/P1 fixes.
Reviewer: Claude Code | Original report by 付佳豪 after qa24 era.
